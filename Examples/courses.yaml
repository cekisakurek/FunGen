name: Courses
states:
  - name: courses
    type: IdentifiedArrayOf<CourseState>
  - name: authToken
    type: String?
actions:
  - name: course
    subtypes:
      - name: id
        type: CourseState.ID
      - name: action
        type: CourseAction
dependencies:
  - course.yaml
